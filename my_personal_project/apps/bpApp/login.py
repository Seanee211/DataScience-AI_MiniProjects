# from flask import Flask, render_template, request

# app = Flask(__name__)

# @app.route('/login', methods=['GET', 'POST'])
# def login():
#     if request.method == 'POST':
#         # 여기서 실제로는 사용자 입력을 받아 인증 절차를 진행해야 합니다.
#         # 이 예시에서는 단순히 로그인 페이지를 보여줍니다.
#         return render_template('login.html')

#     return render_template('login.html')